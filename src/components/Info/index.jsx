import React from 'react';
import { Box } from './styled';

export default function Index() {
  return (
    <Box>
      Esta aplicação foi criada com um propósito didático, pois da forma que
      está agora pode ser meio inútil. Qualquer um consegue criar alunos, apenas
      usuários logados podem editá-los e excluí-los e a rota de criar usuários foi fechada
      por segurança.
    </Box>
  );
}
